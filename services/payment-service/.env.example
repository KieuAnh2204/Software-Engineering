# ==========================================
# Payment Service - Environment Variables
# ==========================================

# Server Configuration
NODE_ENV=development
PORT=3004

# ------------------------------------------
# Database (MongoDB Atlas)
# Replace <db_password> with your actual password
MONGODB_URI=mongodb+srv://foodfast_delivery:<db_password>@cluster0.r3lhqwd.mongodb.net/payment_service?retryWrites=true&w=majority&appName=Cluster0

# ------------------------------------------
# JWT Configuration (optional, only if you verify tokens)
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# ------------------------------------------
# Microservices Communication
# Used when notifying Order Service after successful payment
ORDER_SERVICE_URL=http://localhost:3002

# ------------------------------------------
# VNPay Payment Gateway (Sandbox Configuration)
# These values come from your VNPay merchant email
VNPAY_TMN_CODE=YBI06X9N
VNPAY_HASH_SECRET=VSKKEZZP1UU9D5OB5D7ABG1XAVO1D2N7   # NEVER commit the real one
VNPAY_API_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html

# Return and IPN URLs (must match what you configure in VNPay Merchant Admin)
VNPAY_RETURN_URL=http://localhost:3004/api/payments/vnpay-return
VNPAY_IPN_URL=http://localhost:3004/api/payments/vnpay-ipn

# ------------------------------------------
# CORS (Optional)
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000
