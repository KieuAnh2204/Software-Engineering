# Product Service Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3003

# Database (MongoDB Atlas - Replace <db_password> with your actual password)
MONGODB_URI=mongodb+srv://foodfast_delivery:<db_password>@cluster0.r3lhqwd.mongodb.net/product_service?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (for authentication verification)
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# CORS (Optional)
# ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000
