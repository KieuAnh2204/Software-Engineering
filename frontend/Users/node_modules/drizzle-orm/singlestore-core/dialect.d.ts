import { entityKind } from "drizzle-orm/entityentity";
import type { MigrationConfig, MigrationMeta } from "drizzle-orm/migratorgrator";
import { type BuildRelationalQueryResult, type DBQueryConfig, type Relation, type TableRelationalConfig, type TablesRelationalConfig } from "drizzle-orm/relationsations";
import type { QueryWithTypings } from "drizzle-orm/sql/sqlql/sql";
import { SQL } from "drizzle-orm/sql/sqlql/sql";
import { type Casing, type UpdateSet } from "drizzle-orm/utils/utils";
import { SingleStoreColumn } from "drizzle-orm/singlestore-core/columns/commontore-core/columns/common";
import type { SingleStoreDeleteConfig } from "drizzle-orm/singlestore-core/query-builders/deletere/query-builders/delete";
import type { SingleStoreInsertConfig } from "drizzle-orm/singlestore-core/query-builders/insertre/query-builders/insert";
import type { SingleStoreSelectConfig } from "drizzle-orm/singlestore-core/query-builders/select.typesry-builders/select.types";
import type { SingleStoreUpdateConfig } from "drizzle-orm/singlestore-core/query-builders/updatere/query-builders/update";
import type { SingleStoreSession } from "drizzle-orm/singlestore-core/sessionsinglestore-core/session";
import { SingleStoreTable } from "drizzle-orm/singlestore-core/tablem/singlestore-core/table";
export interface SingleStoreDialectConfig {
    casing?: Casing;
}
export declare class SingleStoreDialect {
    static readonly [entityKind]: string;
    constructor(config?: SingleStoreDialectConfig);
    migrate(migrations: MigrationMeta[], session: SingleStoreSession, config: Omit<MigrationConfig, 'migrationsSchema'>): Promise<void>;
    escapeName(name: string): string;
    escapeParam(_num: number): string;
    escapeString(str: string): string;
    private buildWithCTE;
    buildDeleteQuery({ table, where, returning, withList, limit, orderBy }: SingleStoreDeleteConfig): SQL;
    buildUpdateSet(table: SingleStoreTable, set: UpdateSet): SQL;
    buildUpdateQuery({ table, set, where, returning, withList, limit, orderBy }: SingleStoreUpdateConfig): SQL;
    /**
     * Builds selection SQL with provided fields/expressions
     *
     * Examples:
     *
     * `select <selection> from`
     *
     * `insert ... returning <selection>`
     *
     * If `isSingleTable` is true, then columns won't be prefixed with table name
     */
    private buildSelection;
    private buildLimit;
    private buildOrderBy;
    buildSelectQuery({ withList, fields, fieldsFlat, where, having, table, joins, orderBy, groupBy, limit, offset, lockingClause, distinct, setOperators, }: SingleStoreSelectConfig): SQL;
    buildSetOperations(leftSelect: SQL, setOperators: SingleStoreSelectConfig['setOperators']): SQL;
    buildSetOperationQuery({ leftSelect, setOperator: { type, isAll, rightSelect, limit, orderBy, offset }, }: {
        leftSelect: SQL;
        setOperator: SingleStoreSelectConfig['setOperators'][number];
    }): SQL;
    buildInsertQuery({ table, values, ignore, onConflict }: SingleStoreInsertConfig): {
        sql: SQL;
        generatedIds: Record<string, unknown>[];
    };
    sqlToQuery(sql: SQL, invokeSource?: 'indexes' | undefined): QueryWithTypings;
    buildRelationalQuery({ fullSchema, schema, tableNamesMap, table, tableConfig, queryConfig: config, tableAlias, nestedQueryRelation, joinOn, }: {
        fullSchema: Record<string, unknown>;
        schema: TablesRelationalConfig;
        tableNamesMap: Record<string, string>;
        table: SingleStoreTable;
        tableConfig: TableRelationalConfig;
        queryConfig: true | DBQueryConfig<'many', true>;
        tableAlias: string;
        nestedQueryRelation?: Relation;
        joinOn?: SQL;
    }): BuildRelationalQueryResult<SingleStoreTable, SingleStoreColumn>;
}
